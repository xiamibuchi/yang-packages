{
  "name": "@syseven/video-player",
  "version": "0.0.1",
  "description": "video player based on hls.js",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run opt:icons && vite build && pnpm run build:types",
    "opt:icons": "svgo ./src/icons/*.svg",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "eventemitter3": "^5.0.1",
    "hls.js": "1.5.6"
  }
}
